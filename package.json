{
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsc index.ts --target ES2020 --module commonjs --lib ES2020,DOM --outDir ./ --strict --esModuleInterop --skipLibCheck --resolveJsonModule --moduleResolution node"
  }
}